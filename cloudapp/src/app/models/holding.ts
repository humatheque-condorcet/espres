export interface Holding {
  mms_id: string;
  holding_id: string;
  anies: any;
  link: string;
  title: string;
  author: string;
  call_number_value: string;
  call_number_prefix: string;
  call_number_type: string;
  location: string;
}